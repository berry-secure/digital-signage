import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "pl.signaldeck.player",
  appName: "Signal Deck Player",
  webDir: "dist"
};

export default config;
